import { Component } from '@angular/core';

require('./d-spinner.less');

@Component({
    selector: 'd-spinner',
    template: '<div class="d-spinner"></div>'
})
export class dSpinnerComponent {
    name = 'd-spinner';
}